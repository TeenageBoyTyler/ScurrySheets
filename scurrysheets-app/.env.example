# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google OAuth Configuration (for Supabase Auth setup, not directly used in frontend)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit real credentials to the repository
# 2. Create a separate .env file for your actual values (it's gitignored)
# 3. For GitHub Codespaces, use repository secrets and environment variables
# 4. The Google Cloud Vision API key is provided by each user during app usage,
#    not stored in this .env file